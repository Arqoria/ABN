import { SeriesClient } from "./series-client";

// Voir docs/Tasks.md, Partie C — statut vérifié par dashboard/layout.tsx,
// rôle Admin vérifié côté client dans series-client.tsx.
export default function SeriesPage() {
  return <SeriesClient />;
}
