# Copier vers .env.local (jamais commité) et remplir avec les valeurs de
# Settings → API sur dashboard.supabase.com. NEXT_PUBLIC_* est exposé au
# navigateur — n'y mettre que l'URL et la clé "Publishable" (ex-anon).
# La clé "Secret" (ex-service_role) ne doit JAMAIS avoir de préfixe
# NEXT_PUBLIC_ et ne doit être utilisée que côté serveur (Vercel env vars).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Clé Secret (ex-service_role) — CONTOURNE TOUTE RLS. Utilisée uniquement
# côté serveur (src/lib/supabase/admin.ts, Server Actions déjà protégées
# par une vérification de rôle Admin). JAMAIS de préfixe NEXT_PUBLIC_ ici.
# À définir aussi dans les variables d'environnement Vercel (jamais commitée).
SUPABASE_SERVICE_ROLE_KEY=

# Secret partagé pour protéger /api/cron/generer-occurrences (Partie B,
# génération des occurrences de série) — seule barrière de cette route, pas
# de session utilisateur possible pour un job Vercel Cron. Générer une
# valeur aléatoire longue (ex. `openssl rand -hex 32`), la définir ici ET
# dans les variables d'environnement Vercel. Vercel Cron envoie
# automatiquement `Authorization: Bearer <CRON_SECRET>` sur les routes
# listées dans vercel.json — rien à configurer côté Vercel au-delà de la
# variable d'environnement.
CRON_SECRET=

# Clé API OpenRouteService (Directions, profil foot-walking) — trace le
# circuit planifié en suivant les rues réelles plutôt qu'à vol d'oiseau.
# Utilisée uniquement côté serveur (src/lib/ors.ts, appelée depuis
# src/lib/actions/circuits.ts). Compte gratuit à créer sur
# https://openrouteservice.org/dev/#/signup (email, pas de CB), puis
# "Request a token" → plan Free, service Directions (ou Standard, qui
# l'inclut). Absente ou invalide : dégrade proprement (voir docs/Specs.md),
# jamais d'erreur bloquante.
ORS_API_KEY=
